import EStyleSheet from 'react-native-extended-stylesheet'

export default EStyleSheet.create({
    virionBalance: {
        fontSize: 22,
        fontWeight: '600',
        color: '$white',
        marginTop: 15
    },
    virionPerSecond: {
        fontSize: 15,
        color: '$white'
    }
})